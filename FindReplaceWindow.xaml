<local:ThemedWindow x:Class="Jester.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Jester"
        Style="{StaticResource JesterWindowStyle}"
        Title="Find" SizeToContent="Height" Width="450"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        PreviewKeyDown="Window_PreviewKeyDown">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Right" Width="104" Margin="12,0,0,0">
            <Button Content="_Find Next" Click="FindNext_Click" IsDefault="True" Margin="0,0,0,6" Padding="0,4" />
            <Button x:Name="ReplaceButton" Content="_Replace" Click="Replace_Click" Margin="0,0,0,6" Padding="0,4" />
            <Button x:Name="ReplaceAllButton" Content="Replace _All" Click="ReplaceAll_Click" Margin="0,0,0,6" Padding="0,4" />
            <Button Content="Cancel" Click="Cancel_Click" Padding="0,4" />
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Find what:" VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox x:Name="FindBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Padding="2,3"
                     TextChanged="FindBox_TextChanged" />

            <TextBlock x:Name="ReplaceLabel" Grid.Row="1" Grid.Column="0" Text="Replace with:"
                       VerticalAlignment="Center" Margin="0,0,8,8" />
            <TextBox x:Name="ReplaceBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" Padding="2,3" />

            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
                <CheckBox x:Name="MatchCaseBox" Content="Match _case" Margin="0,0,18,0" VerticalAlignment="Center" />
                <CheckBox x:Name="WrapBox" Content="_Wrap around" IsChecked="True" Margin="0,0,18,0" VerticalAlignment="Center" />
                <TextBlock Text="Direction:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <RadioButton x:Name="UpRadio" Content="_Up" GroupName="Dir" Margin="0,0,10,0" VerticalAlignment="Center" />
                <RadioButton x:Name="DownRadio" Content="_Down" GroupName="Dir" IsChecked="True" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </DockPanel>
</local:ThemedWindow>
